{
  "name": "@matrix-publisher/core",
  "private": true,
  "version": "0.1.0",
  "description": "Shared core types, schemas, constants, utils and API layer",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "echo 'TODO: core tests' && exit 0"
  },
  "devDependencies": {
    "@matrix-publisher/config": "workspace:*",
    "typescript": "^5.6.3"
  }
}
